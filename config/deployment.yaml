apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose-resolved.yaml -c
    kompose.version: 1.30.0 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: frontend
  name: frontend
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: frontend
  strategy: {}
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose-resolved.yaml -c
        kompose.version: 1.30.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/deals-backend-default: "true"
        io.kompose.service: frontend
    spec:
      containers:
        - image: registry.digitalocean.com/marsbase/frontend
          name: frontend
          ports:
            - containerPort: 80
              hostPort: 3000
              protocol: TCP
          resources: {}
      restartPolicy: Always
status: {}
