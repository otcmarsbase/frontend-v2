import {
	Thead,
	Tr,
	TableContainer,
	Table as T,
	Tbody,
	Th,
	Td,
} from '@chakra-ui/react'
import React from 'react'

type TableProps = {}

export const Table: React.FC<TableProps> = ({}) => {
    return <div></div>
}