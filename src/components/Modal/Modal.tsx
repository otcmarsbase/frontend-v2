import { ModalBody, Modal as ModalWrapper, ModalContent } from '@chakra-ui/react'
import React from 'react'

type ModalProps = {}

export const Modal: React.FC<ModalProps> = ({}) => {
    return <div>
        <ModalContent ></ModalContent>
    </div>
}