import { Theme } from '@nivo/core';

export default {
  legends: {
    text: {
      fontFamily: 'Inter, sans-serif',
      fontSize: '0.875rem',
    },
  },
  annotations: {
    symbol: {
      borderRadius: '2rem',
    },
  },
} as Theme;
